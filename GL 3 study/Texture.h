#ifndef TEXTURE_H
#define TEXTURE_H

#pragma once
#include <GL\glut.h>
#include <string>

class Texture
{
public:
    //Texture(GLenum TextureTarget, const std::string& FileName);
 
    //bool Load();
 
    //void Bind(GLenum TextureUnit);
};

#endif /*TEXTURE*/